import UIKit

class Measurement {
    
    // Measurement array for Firebase
    var measure: String?
    var date: String?
    init(measure: String?, date: String?) {
        self.measure = measure
        self.date = date }
}
