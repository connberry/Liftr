import UIKit

class Calc {
    
    // Calculator array for Firebase
    var calc: String?
    var date: String?
    init(calc: String?, date: String?) {
        self.calc = calc
        self.date = date }
}
